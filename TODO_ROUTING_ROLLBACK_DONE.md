Completed routing rollback (best-effort):

- Updated `src/App.tsx` to remove React Router routing configuration.
- Updated `src/main.tsx` to remove `BrowserRouter` wrapper and `react-router-dom` usage.

Build succeeded.

